# Optional. Powers only the "Ask about this step" tutor panel.
# Without it that panel reports it is not configured and everything else —
# traces, invariants, divergence, complexity — works exactly the same.
ANTHROPIC_API_KEY=
